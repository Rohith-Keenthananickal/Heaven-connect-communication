"""
Entry point for Heaven Connect Communication Server
Run this file to start the FastAPI server
"""

from app.main import app

if __name__ == "__main__":
    import uvicorn
    uvicorn.run("app.main:app", host="0.0.0.0", port=8000, reload=True)